import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // PWA configuration would be added via next-pwa for production deployment
  // next-pwa with App Router requires specific setup
  experimental: {},
};

export default nextConfig;
